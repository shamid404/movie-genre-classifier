# movie-genre-classifier
A production-ready multi-label Movie Genre Prediction System using TF-IDF + Logistic Regression, built with FastAPI, scikit-learn, and Docker. Trained on 160K+ movies and deployed to Fly.io with &lt;100ms API responses.
